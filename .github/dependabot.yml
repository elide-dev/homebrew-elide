version: 2
updates:
  # Keeps the SHA-pinned actions in ci.yml current. Grouped and weekly so the
  # tap's PR list stays dominated by formula bumps, not tooling churn.
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns:
          - "*"
